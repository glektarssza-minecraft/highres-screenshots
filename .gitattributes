# Treat all files as text files with Unix-style line endings by default
* text eol=lf

# Treat .bat files as text files with Windows-style line endings
*.bat text eol=crlf

# Treat .cmd files as text files with Windows-style line endings
*.cmd text eol=crlf

# All .jar files are treated as binary files
*.jar binary

# Run all .jar files through Git LFS
*.jar filter=lfs diff=lfs merge=lfs -text
